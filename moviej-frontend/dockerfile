# 빌드 단계
FROM node:18 as build

WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치
RUN npm install

# 소스 코드 복사
COPY . .

# 애플리케이션 빌드 (output: 'export' 설정으로 정적 파일 생성)
RUN npm run build

# 실행 단계
FROM nginx:alpine

# 빌드된 파일을 nginx로 복사 (Next.js export 결과물은 out 폴더에 생성됨)
COPY --from=build /app/out /usr/share/nginx/html

# nginx 설정 복사 (SPA 라우팅을 위해)
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 포트 노출
EXPOSE 80

# nginx 시작
CMD ["nginx", "-g", "daemon off;"]